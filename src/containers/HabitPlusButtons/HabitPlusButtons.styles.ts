import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  habitPlusButtonsContainer: {
    position: 'absolute',
    bottom: 20,
    right: 15,
  },
  habitPlusButton: {
    alignItems: 'flex-end',
  },
});
