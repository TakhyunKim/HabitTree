import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  growingTreeContainer: {
    position: 'relative',
    width: '100%',
    height: '100%',
    alignItems: 'center',
    paddingTop: 70,
  },
});
