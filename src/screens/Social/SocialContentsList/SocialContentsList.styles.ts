import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  socialContentsListContainer: {
    width: '90%',
    height: '100%',
    justifyContent: 'flex-start',
    alignItems: 'center',
    margin: 20,
  },
});
