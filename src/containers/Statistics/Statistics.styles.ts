import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  statisticsContainer: {
    width: '90%',
    height: '100%',
  },
  waterCanStatisticsWrapper: {
    height: 100,
    flexDirection: 'row',
    alignItems: 'center',
  },
});
