import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  habitPlusSubmitButtonContainer: {
    position: 'absolute',
    bottom: 0,
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: '13%',
    backgroundColor: '#45E456',
  },
  submitButtonTitleText: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#414141',
  },
});
